import { FormHighlightDirective } from './form-highlight.directive';

describe('FormHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FormHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
